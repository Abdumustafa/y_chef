import 'package:flutter/material.dart';

class health_food  extends StatelessWidget {
  const health_food({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("heath food"),
        centerTitle: true,
      ),
    );
  }
}
