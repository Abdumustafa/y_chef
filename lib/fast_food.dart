import 'package:flutter/material.dart';

class fast_food  extends StatelessWidget {
  const fast_food ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("fast food"),
        centerTitle: true,
      ),
    );
  }
}
